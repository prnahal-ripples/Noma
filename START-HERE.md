# START HERE (for the human, not the AI)

This folder is the knowledge skeleton for your AI-first app. Nothing here is code yet —
it is the "source of truth" structure that both you and AI agents will fill in and build against.

The system in one line:
CLAUDE.md routes → memory/ holds → docs/ plans → .claude/rules/ enforces → src/ is canonical machine truth.

Your next steps:
1. Put this folder somewhere permanent (e.g. Documents/Projects/).
2. Turn it into a Git repository and push it to GitHub (Claude can walk you through this).
3. Open the folder with Claude Code and run the "build the memory system" prompt
   (from your chat thread "Building app memory from architecture reference") — or start
   filling in the TODO(owner) items in memory/product/ yourself, beginning with vision.md and personas.md.

Every file marked "STUB" contains a note about what belongs in it.
